<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hadith_collection.hadith_books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><a href="/hadith_books/<%= book.id %>">Show</a></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_hadith_collection_path(@hadith_collection) %> |
<%= link_to 'Back', hadith_collections_path %>
