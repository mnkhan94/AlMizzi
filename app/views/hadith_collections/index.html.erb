<p id="notice"><%= notice %></p>

<h1>Listing Hadith Collections</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hadith_collections.each do |hadith_collection| %>
      <tr>
        <td><%= link_to 'Show', hadith_collection %></td>
        <td><%= link_to 'Edit', edit_hadith_collection_path(hadith_collection) %></td>
        <td><%= link_to 'Destroy', hadith_collection, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hadith collection', new_hadith_collection_path %>
