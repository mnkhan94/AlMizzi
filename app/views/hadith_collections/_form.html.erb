<%= form_for(@hadith_collection) do |f| %>
  <% if @hadith_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@hadith_collection.errors.count, "error") %> prohibited this hadith_collection from being saved:</h2>

      <ul>
      <% @hadith_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "form-control" %>
  </div>
  <div class="field">
    <%= f.label :author %><br>
    <%= f.text_area :author, :class => "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
