<%= form_for(@narrator) do |f| %>
  <% if @narrator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@narrator.errors.count, "error") %> prohibited this narrator from being saved:</h2>

      <ul>
      <% @narrator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :full_name %><br>
    <%= f.text_field :full_name, :class => "form-control" %>
  </div>


  <br>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
